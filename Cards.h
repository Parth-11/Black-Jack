#include <vector>
#include <string>

class Cards{
    private:
        std::vector<std::vector<int>> Matrix;
        std::vector<std::string> cardType;
        std::vector<int> cardNum;
    public:
        
};